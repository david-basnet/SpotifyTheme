<div class="featured-kid-products">
  <h2 class="featured-kid-heading">{{ section.settings.heading }}</h2>
  <div class="featured-kid-products-wrapper">
    <div class="featured-kid-grid">
      {% for block in section.blocks %}
        {% assign product = all_products[block.settings.product] %}
        <div class="featured-kid-card" {{ block.shopify_attributes }}>
          {% if product and product.featured_image %}
            <img src="{{ product.featured_image | img_url: '400x' }}" alt="{{ product.title }}" class="featured-kid-img" loading="lazy">
          {% endif %}
          <div class="featured-kid-info">
            <div class="featured-kid-title">{{ product.title }}</div>
            <div class="featured-kid-price">{{ product.price | money }}</div>
            <a href="/products/{{ product.handle }}" class="featured-kid-link">Shop Now</a>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

<style>
.featured-kid-products { padding: 32px 0; background: #fff; }
.featured-kid-heading { text-align: center; margin-bottom: 24px; font-size: 2rem; font-weight: bold; }
.featured-kid-products-wrapper { max-width: 1120px; margin: 0 auto; }
.featured-kid-grid { display: flex; gap: 32px; justify-content: center; flex-wrap: wrap; }
.featured-kid-card { width: 200px; background: #f7f7f7; border-radius: 10px; overflow: hidden; display: flex; flex-direction: column; align-items: center; box-shadow: 0 2px 8px rgba(0,0,0,0.04); }
.featured-kid-img { width: 100%; height: 160px; object-fit: contain; background: #fff; }
.featured-kid-info { padding: 12px; text-align: center; }
.featured-kid-title { font-size: 1rem; font-weight: bold; margin-bottom: 6px; }
.featured-kid-price { font-size: 1.1rem; font-weight: bold; color: #333; margin-bottom: 8px; }
.featured-kid-link { color: #fff; background: #111; border-radius: 4px; padding: 8px 16px; text-decoration: none; font-size: 0.95rem; display: inline-block; margin-bottom: 8px; }
.featured-kid-link:hover { background: #0071e3; }
</style>

{% schema %}
{
  "name": "Featured Kid Products",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Featured Kid's Products"
    }
  ],
  "blocks": [
    {
      "type": "product",
      "name": "Featured Product",
      "limit": 6,
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Select Product"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Featured Kid Products",
      "blocks": []
    }
  ]
}
{% endschema %} 